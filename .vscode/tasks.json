{
    "version": "2.0.0",
    "tasks": [
      {
        "type": "typescript",
        "tsconfig": "anchor/tsconfig.spec.json",
        "problemMatcher": [
          "$tsc"
        ],
        "group": "build",
        "label": "tsc: build - tsconfig.spec.json",
        "options": {
          "cwd": "${workspaceFolder}/anchor"
        }
      },
      {
        "label": "prepare-validator",
        "type": "shell",
        "command": "rm -rf ${workspaceFolder}/anchor/.anchor/test-ledger && cd ${workspaceFolder}/anchor && RUSTFLAGS=\"-g\" anchor build",
        "problemMatcher": [],
        "group": "build"
      }
    ]
  }